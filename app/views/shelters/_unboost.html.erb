<%= form_for(current_user.favorites.find_by_shelter_id(@shelter), html: { method: :delete }, remote: true) do |f| %>
  <%= f.submit "Following Us", class: "btn btn-header btn-follow btn-unfollow btn-info" %>
<% end %>