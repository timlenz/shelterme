<div id="mainError" class="noPrint"> <!-- ERROR ALERTS GO HERE -->
  <% flash.each do |key, value| %>
    <div class="errorBox">
      <button class="close" data-dismiss="alert">&times;</button>
      <%= content_tag(:div, value, class: "alert alert-#{key}") %>
    </div>
  <% end %>
</div>