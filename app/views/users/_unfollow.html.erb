<%= form_for(current_user.relationships.find_by_followed_id(@user),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Following", :class => "btn btn-header btn-follow btn-unfollow btn-info" %>
<% end %>