<div class="social">
  <h3><%= image_tag("shareMe.png", alt: "Share Me", class: "noPrint") %><span class="print">Share Me</span></h3>
  <% render 'facebook' %>
  <% render 'twitter' %>
  <% render 'googleplus' %>
  <div class="socialItem noPrint">
    <a class="btn btn-mini btn-print" href="JavaScript:window.print();"><i class="icon-print"></i> Print</a>
  </div>
  <% render 'pinterest' %>
  <div class="socialItem short">
    <%= simple_form_for @pet, :html => {style: "float:left;"} do |f| %>
    <% tempURL = "http://api.bitly.com/v3/shorten?login=timlenz&apiKey=R_d75522808cf11eb400f96d8b5478bc2b&longUrl=#{u(request.protocol+request.host_with_port+request.fullpath)}&format=txt" %>
    <!--< % shortURL = open("#{tempURL}").read % >-->
    <%= f.input :text, label: false, input_html: { value: "# {tempURL}" }, readonly: true %>
    <% end %>
  </div>
  <div class="clearfix"></div>
</div>