As of <%= Time.now.strftime("%I:%M %p on %b %d, %Y") %>, could not find <%= @pet.name ? @pet.name.titleize + " " : "" %>(<%= @pet.animal_code %>) at <%= @pet.shelter.name %>.

Submitted by <%= @user.name %> (<%= @user.email %>).